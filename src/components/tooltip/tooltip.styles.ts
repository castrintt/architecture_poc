import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    tooltipContentStyle: {
        backgroundColor: '#000',
        width: 'auto',
        height: 'auto',
        padding: 20
    },
    tooltipText: {
        color: 'white',
        fontSize: 16,
        textAlign: 'center',
    },

});